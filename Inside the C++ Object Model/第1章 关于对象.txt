第一章 关于对象


加上封装后的布局成本
    一个坐标类由C实现改为C++实现，实际上并没有增加成本，因为虽然在类中声明了成员函数，但是
    对于非内联的成员函数，每个类只会诞生一个他的函数实例，而非每个对象都拥有一个，而内联成
    员函数则会在这个类型的所有对象中都包含一个函数实例
    在本章随后的内容中，你将看到在布局和存取时间上主要的额外负担是由 virtual 引起的，包括
    虚函数和虚基类